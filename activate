export PATH="${PATH}:$(readlink -f ~/Desktop/opensim-core/opensim-core-build/)"

# enables running `perf stat` without having to `sudo` the opensim cmd
sudo bash -c 'echo "-1" > "/proc/sys/kernel/perf_event_paranoid"'
